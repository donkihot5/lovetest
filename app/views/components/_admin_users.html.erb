<!--check this page for errors, fix links to the users profile-->

<% if current_user.id != user.id %>    
    <div class="card border-secondary mb-1">
            <div class="card-body">
            <h5 style="color: grey; font-size: 125%;">Edit User</h5>
                <div class="col-12">
                    <div class="row mb-2">  
                      <%= link_to "New User", new_user_path, class: "btn btn-outline-primary btn-sm btn-wid" %>
                    </div>
                    <div class="row mb-2">      
                      <%= link_to "Edit User", edit_user_path, class: "btn btn-outline-primary btn-sm btn-wid" %>
                    </div>
                    <div class="row mb-2">    
                      <%= link_to "Delete User", user_path, method: :delete, class: "btn btn-outline-primary btn-sm btn-wid" %>
                    </div>                
                </div>
            </div>
        </div>                       
<% end %>

        